"""
Strategy execution engine package.

Contains the StrategyExecutionEngine — the core loop that ties together
market data ingestion, indicator computation, signal generation,
evaluation through risk gates, and order submission.
"""
